#include<iostream>
using namespace std;
void Name(){   // function defination and syntax
    cout<<"Mein hoon Gabbar";  //void is the return type
}                               //Name is the function name
int main(){
    Name();   // we are calling the function
}

//Generally we write function above main
// main fn is always called (default), but for other fn we need to call